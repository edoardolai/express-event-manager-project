<!-- START OF CODE WRITTEN WITHOUT ASSISTANCE -->

<%- include('../partials/header') %>

    <main class="max-w-3xl mx-auto p-6">
        <div class="bg-white shadow-md rounded-lg p-8">
            <!-- Header -->
            <div class="mb-2">
                <span class="text-sm text-gray-500 uppercase tracking-wider">Configuration</span>
            </div>
            <h1 class="text-3xl font-bold text-gray-900 mb-6">Site Settings</h1>

            <%- include("../partials/error.ejs", { errorMessage: errorMessage }) %>

                <form action="/organizers/site-settings" method="POST" class="space-y-6">
                    <div>
                        <label class="block text-gray-700 mb-2">Site Name</label>
                        <input type="text" name="siteName" value="<%=siteName || locals.siteName%>"
                            class="w-full p-2 border rounded">
                    </div>

                    <div>
                        <label class="block text-gray-700 mb-2">Site Description</label>
                        <textarea name="siteDescription" rows="4"
                            class="w-full p-2 border rounded"><%=siteDescription %></textarea>
                    </div>

                    <div class="flex justify-between pt-6">
                        <a href="/organizers/home" class="text-gray-600 px-4 py-2 rounded hover:text-red-600/65  h-fit">
                            Cancel
                        </a>
                        <button type="submit" class="px-6 py-2 bg-green-600 text-white rounded hover:bg-green-700">
                            Save Changes
                        </button>
                    </div>
                </form>
        </div>
    </main>

    <%- include('../partials/footer') %>
        <!-- END OF CODE WRITTEN WITHOUT ASSISTANCE -->